/// Widget keys for the random dog-image surface, shared with its widget and
/// integration tests.
class DogImageRandomKeys {
  DogImageRandomKeys._();

  static const String scaffold = 'dogImageRandom.scaffold';
  static const String loadButton = 'dogImageRandom.loadButton';
  static const String saveButton = 'dogImageRandom.saveButton';
  static const String result = 'dogImageRandom.result';
}
